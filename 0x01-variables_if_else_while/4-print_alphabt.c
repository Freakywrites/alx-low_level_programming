#include <stdio.h>
#include <stdlib.h>
#include<time.h>
/**
 * main -Entry point
 *
 * Return: Always return 0 (Success)
 */

int main(void)
{
	char low, e, q;

	e = 'e';
	q = 'q';

	for (alp = 'a'; alp <= 'z'; alp++)
	{
		if (alph != e && alp != 0)
			putchar(alp)
	}
}
